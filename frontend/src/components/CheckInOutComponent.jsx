import { DateRangePicker } from "@nextui-org/react";

export const CheckInOutComponent = () => {

  return (
    <DateRangePicker
      radius="full"
      label="Check in - Check out"
      className="max-w-xs"
    />
  )
}
