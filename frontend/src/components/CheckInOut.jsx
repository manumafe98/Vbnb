import {DateRangePicker} from "@nextui-org/react";

const CheckInOut = () => {
  return (
    <DateRangePicker 
      label="Check in - Check out"
      className="max-w-xs" 
    />
  );
}

export default CheckInOut
